# Gridlock 2.0 - Traffic Violation AI System Requirements
# Optimized for Streamlit Cloud deployment (CPU-based inference)

# Computer vision and image processing (must be first to prevent opencv-python from being installed)
opencv-python-headless>=4.8.0,<5.0.0

# Core framework
streamlit>=1.30.0,<2.0.0

# Deep learning frameworks (CPU versions for Streamlit Cloud compatibility)
torch>=2.3.0,<2.5.0
torchvision>=0.18.0,<0.20.0

# Object detection (YOLOv8) - installed without dependencies to prevent opencv-python
ultralytics>=8.1.0,<9.0.0

# Ultralytics dependencies (excluding opencv-python)
matplotlib>=3.3.0
pillow>=8.0.0
pyyaml>=5.3.1
requests>=2.23.0
scipy>=1.4.1
tqdm>=4.64.0
pandas>=1.1.4
seaborn>=0.11.0

# OCR for license plate reading
easyocr>=1.7.1,<2.0.0

# Image processing
numpy>=1.24.0,<2.0.0
Pillow>=10.0.0,<11.0.0

# PDF generation
fpdf2>=2.7.0,<3.0.0

# Configuration and utilities
PyYAML>=6.0,<7.0